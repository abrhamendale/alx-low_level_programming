#include <unistd.h>
#include "main.h"
#include <stdlib.h>

/**
 * _abs -Entry point
 *
 * Description: Prints the absolute value
 * @n: Argument
 *
 * Return: 0.
 */
int _abs(int n)
{
	return (abs(n));
}
