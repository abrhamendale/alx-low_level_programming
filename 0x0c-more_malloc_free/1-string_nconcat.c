#include "main.h"

/**
 * malloc_checked - Allocates memory using malloc
 *
 *
 * Retur: Created string
 */
char *string_nconcat(char *s1, char *s2, unsigned int n)
{
	int i, j;

	if (malloc(b) == NULL)
	{
		exit (98);
	}
	return (malloc(b));
}
